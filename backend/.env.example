# Database (Free - run locally with Docker)
DATABASE_URL=postgresql://skillsense:changeme@localhost:5432/skillsense

# Redis for Celery (Free - run locally with Docker)
REDIS_URL=redis://localhost:6379/0

# GitHub API (Free tier - 5,000 requests/hour)
# How to get it:
# 1. Go to https://github.com/settings/tokens
# 2. Generate new token (classic)
# 3. Select scopes: repo (for private repos) or public_repo (for public only)
# 4. Copy the token here
GITHUB_TOKEN=your_github_personal_access_token_here

# Sanity (GROQ API) - PAID SERVICE - Comment out or leave empty for local fallback
# SANITY_PROJECT=your-sanity-project-id
# SANITY_DATASET=production
# SANITY_TOKEN=your-sanity-token

# OpenAI - PAID SERVICE - Leave empty to use local LLM fallback
# OPENAI_API_KEY=your-openai-api-key

# LinkedIn - NOT FREE - Leave empty, app will use manual text input instead
# LINKEDIN_COOKIE=your-linkedin-session-cookie
